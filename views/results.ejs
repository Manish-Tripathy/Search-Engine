<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Search Results</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    .container {
      max-width: 1200px;
      margin: 30px auto;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #ffcc00;
    }

    .problem-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 20px;
      margin-top: 30px;
    }

    .problem-card {
      background: #000;
      color: #fff;
      padding: 20px;
      border: 2px solid #ff5050;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(255, 0, 0, 0.3);
      transition: transform 0.2s ease;
    }

    .problem-card:hover {
      transform: translateY(-4px);
    }

    .problem-title {
      font-size: 1.3rem;
      font-weight: bold;
      color: #ff5050;
    }

    .problem-details {
      margin-top: 10px;
      font-size: 0.95rem;
    }

    .problem-links a {
      color: #00ccff;
      margin-right: 10px;
      text-decoration: none;
      font-weight: bold;
    }

    .problem-links a:hover {
      text-decoration: underline;
    }

    .back-button {
      display: inline-block;
      margin-top: 30px;
      padding: 10px 15px;
      background: #ffcc00;
      color: black;
      text-decoration: none;
      font-weight: bold;
      border-radius: 5px;
    }

    .back-button:hover {
      background: #ff5050;
      color: white;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Search Results for "<%= query %>"</h1>

    <div class="problem-grid">
      <% results.forEach(problem => { %>
        <div class="problem-card">
          <div class="problem-title"><%= problem.name %></div>
          <div class="problem-details">
            <p><strong>Code:</strong> <%= problem.code %></p>
            <p><strong>Difficulty:</strong> <%= problem.difficulty %></p>
            <!-- <p><strong>Similarity Score:</strong> <%= problem.score %></p> -->
            <div class="problem-links">
              <a href="/problem/<%= problem.code %>">View</a>
              <a href="https://www.codechef.com/problems/<%= problem.code %>" target="_blank">Problem Link</a>
              <a href="https://discuss.codechef.com/t/<%= problem.code.toLowerCase() %>-editorial" target="_blank">Editorial</a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>

    <a href="/" class="back-button">← Back to Search</a>
  </div>
</body>
</html>
